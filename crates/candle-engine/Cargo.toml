[package]
name = "tradeview-candle-engine"
version = "0.1.0"
edition = "2021"

[dependencies]
tradeview-domain = { path = "../domain" }
tracing.workspace = true

[dev-dependencies]
rust_decimal.workspace = true
